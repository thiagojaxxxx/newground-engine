function calculate(input) {
  return {
    result: { score: 0 },
    reasons: ['placeholder'],
    assumptions: ['rules not implemented'],
    ruleVersion: 'v0'
  };
}

module.exports = { calculate };

